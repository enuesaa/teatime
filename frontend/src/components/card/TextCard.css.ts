import { style } from '@vanilla-extract/css'

export default {
  text: style({
    textAlign: 'left',
    height: '70%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  }),
}